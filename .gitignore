node_modules
dist
dist-ssr
*.local
*.tsbuildinfo
.DS_Store
.vite
.claude/settings.local.json

# workflow tab files (user data, not code)
workflows/

# ไฟล์ตัวอย่างที่ export ไว้ดู (ไม่ใช่โค้ด)
ตัวอย่าง-export/
